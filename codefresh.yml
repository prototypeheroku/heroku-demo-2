version: '1.0'
steps:



 unit-tests:
      image: node:latest
      fail-fast: false
      working-directory : ${{initial-clone}}
      commands:
        - npm install -g mocha
        - npm test
        - echo $(date)

  deploy:
       image: verchol/heroku:demo
       fail-fast: false
       working-directory : ${{initial-clone}}
       commands:
         - heroku status
         - git push heroku master
